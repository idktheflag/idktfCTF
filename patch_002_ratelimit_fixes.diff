--- a/src/auth/ratelimit.rs
+++ b/src/auth/ratelimit.rs
@@ -1,7 +1,6 @@
 use std::{
     collections::{HashMap, VecDeque},
-    sync::Arc,
     time::{Duration, Instant},
 };

@@ -41,7 +41,7 @@ impl RateLimiter {
         // Record this attempt.
-        attepmts.push_back(now);
+        attempts.push_back(now);
         Ok(())
     }
 }
